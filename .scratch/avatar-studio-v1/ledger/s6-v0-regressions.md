# S6 — V0 regression evidence

Status: audit and verification plan; fresh final-source execution pending. This file did not exist before the audit. Assigned owner `pakmanager_s0` performed read-only source/base/report inspection and wrote only this ledger. Root owns implementation assignments, all builds, editor/process operations, tests and commits. This is the existing Avatar Studio V1 task, not a new product scope or approval request.

Specification: host `Context/V1/Avatar-Studio-V1-Implementation-Design.md` §§6.4, 7, 8.2, 9 and A07/A11/S6; visual design, authorized handoff and full meeting decisions. Preserve V0 Scene/platform workflows and all-label refresh. V1 selected cooking remains separate; Stage/limited-environment work is excluded. Apply plan-implement-review and ponytail: use existing native tests and the existing guarded runner; no new job framework.

## Audited behavior against staging 0218c2b

| Boundary | Current source evidence | Regression implication |
|---|---|---|
| V0 archive defaults | `Publish/CPM_PublishTypes.cpp:181-210`; subsystem `ConvaiPakEditorSubsystem.cpp:1542-1570`; settings header `:119-121` | `ProjectSetting` still computes `!packageOnly && policy && setting`. New Omit/IncludeFresh are explicit per-run options; policy denial refuses before queue construction. Platform selection remains separate. |
| V0 queue/upload selection | subsystem `:1613-1637` | Package job is conditional on selected platforms; archive job uses the same resolved boolean passed to upload configuration. Package-only queues no create/upload/persist job. Source-policy helper tests do not execute this subsystem queue. |
| Source upload timestamp | subsystem `:1720+` | Record is still written only after complete successful run when raw was archived. Only misleading marker prose changed; timestamp is not correspondence evidence. |
| Wire slots and output files | `CPM_PublishTypes.cpp:33-48`; `Utility/CPM_UtilityLibrary.cpp:194-214`; `Jobs/CPM_PublishJobs.cpp:213-219` | Slots remain `ue-5.8-Windows`, `ue-5.8-Linux`, `ue-5.8-Raw` on this engine. Artifact lookup retains `<ProjectDir>/PackagedApp/<platform>/<project>/Content/Paks/pakchunk<id>-<platform>.pak`. Invalid platform produces an empty slot. No edit revision or concurrency token is inferred. |
| V0 label refresh | `ConvaiPakManagerEditorUtils.cpp:49-80,96` | Every discovered PrimaryAssetLabel is unconditionally saved before packaging, with `bOnlyIfIsDirty=false`; there is no selected-chunk filter. Save failures remain warnings. This file is byte-diff unchanged from base. Calling this API in the persistent linked V1 uploader would write linked labels and is forbidden for the selected worker. |
| Cook MCP isolation | same file `:115-120` | UAT retains `-AdditionalCookerOptions=-ini:EditorPerProjectUserSettings:[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]:bAutoStartServer=False`. It changes the cook command only, not host settings. |
| UAT and cancellation | `Jobs/CPM_PublishJobs.cpp:118-139,191-210,222-290`; `Jobs/CPM_PublishRunner.cpp:105-153` | Packaging deletes the prior expected Pak before a fresh cook and checks an output exists after Completed. Cancel only restores session Live Coding state and reports Cancelled through the base job. It does not cancel or drain UAT. These paths are unchanged from base; this is a documented legacy limitation, not a new regression. Do not present WorkerProcess containment tests as proof for this path. Launch regression editors with `-NoLiveCoding`; no compile through Live Coding is authorized. |
| Shared preparation facade | `CPM_DependencyCopyAPI.cpp:9-56,60-93`; public options `bStrictValidation=false` | All existing option/report members are mapped into/out of shared preparation. Enum ordering and old public signatures remain. Strict validation is opt-in. The original suppression option defaults true; shared scoped suppression now also blocks native slow dialogs and restores globals. Existing strict-copy fixtures do not call this reflected CPM facade/default branch. |
| Avatar and thumbnail facade | `Avatar/CPM_AvatarBlueprint.cpp`; `Thumbnail/CPM_Thumbnail.cpp` | Legacy functions delegate to the shared preparation module; asset-type thumbnail shape stays in CPM. Existing CPM avatar/thumbnail tests call these public facade paths. New bounded preview decoder APIs are additive. |

Read-only Git comparison found no changes against `0218c2b` in V0 EditorUtils, Jobs, existing AvatarBlueprint tests or existing Thumbnail tests. Changed V0 execution paths are the preparation facades, Build.cs preparation dependency and explicit source resolution/marker text above. Existing publishing, label and cancellation behavior must still be exercised after module extraction/integration; unchanged source alone is not runtime proof.

Native engine source corroborates why the label check matters: `Engine/Source/Runtime/Engine/Private/DataAsset.cpp:59-68` rebuilds bundles in PreSave; `PrimaryAssetLabel.cpp:45-69` derives the directory bundle. Loading can refresh memory, so a loaded label is insufficient evidence. Use `IAssetRegistry::LoadPackageRegistryData` to inspect saved bundles, as the existing selected-cook fixture already does.

Native UAT source has a distinct cancel action: `Engine/Source/Editor/UATHelper/UATHelperModule.cpp:414,434-436,507-520` owns FSerializedUATProcess and the notification calls `Cancel(true)`. `IUATHelperModule::CreateUatTask` returns void (`Public/IUATHelperModule.h:47`); CPM retains no owned process handle. The notification cancel and CPM CancelPublish are different behaviors.

Native MCP settings `Engine/Plugins/Experimental/ModelContextProtocol/Source/ModelContextProtocolEngine/Private/ModelContextProtocolSettings.cpp:31-49` honor explicit `-ModelContextProtocolStartServer` / deprecated `-StartModelContextProtocolServer` before the INI value. A cook isolation fixture must exclude those overriding flags. Merely passing the false setting does not override an explicit force-start flag.

## Existing tests and exact runnable filters

There are 84 literal `ConvaiPakManager.*` test declarations in the current CPM test directory, versus the coordinator's 74-case pre-change baseline. Test discovery and exported actual results determine execution, not this text count. Broad execution includes additive selected-source/selected-cook cases and an opt-in source-export case; an unrequested opt-in test's successful no-op is not integration evidence.

| Filter | Actual coverage | Limits |
|---|---|---|
| `ConvaiPakManager.Publish.SourcePolicy` | `PreservesLegacyDefaults`, `ExplicitChoiceRespectsPolicy`, `ArchiveReplacesPreviousBytes` | First two are pure typed resolution. Third invokes real `CPM_CreateZip` and native reader on owned text files, checks changed/removal bytes and unchanged local marker. It does not run a publish queue, archive a real avatar, or upload. |
| `ConvaiPakManager.Publish.Policy` | Nine parsing/validation/selection/settings cases, including `PackageOnlyNeverReusesPaks` and `ReadsTheLivePolicy` | `ReadsTheLivePolicy` parses a literal representative JSON policy in C++; its name does not mean a live network request. |
| `ConvaiPakManager.Publish.Metadata` | Scene/avatar document updates, legacy keys/tags, private create, sizes and `NamesEveryVersionSlotTheSameWay` | Native C++ document generation and literal expectations; no backend acceptance, real Pak-size measurement, ownership or update concurrency proof. |
| `ConvaiPakManager.Publish.Response` / `.PakReuse` | Response document parsing and native existing-file rejection | No actual publish/delete endpoint or freshness contract. |
| `ConvaiPakManager.Publish.Runner` | Six real runner tests: order, failure, cancel, single synchronous finish, timeout, progress | `CPM_PublishRunnerTestJob.h` explicitly implements a Fake job. Cancel/timeout exercise real runner plumbing with counters, not UAT/HTTP process termination. |
| `ConvaiPakManager.Avatar` | Seven native SCS/component/MetaHuman-shape setup cases through CPM facade | Loads actual SDK chatbot/animation assets; the importer face class is a transient stand-in (`CPM_AvatarBlueprintTest.cpp:135-147`). This is not a real imported MetaHuman cook or a saved edited avatar round trip. |
| `ConvaiPakManager.Thumbnail` | Five native empty/import/texture/crop/asset-type shape cases through facade | No full preview UI/cook acceptance. |
| `ConvaiPakManager.Chunk` / `.Environment` / `.UI.ViewModel` | Existing bootstrap/migration/reconcile/cleanup, environment partition, and pure view-model behavior | Cleanup tests are local records/files; do not claim real backend delete. |
| `ConvaiPakManager.Preconditions` / `.Compatibility` | Toolchain/version/refusal rules | A parsed version is not a successful Linux/native SDK build. |
| `ConvaiPakManager.Publish.SelectedCook` | Two native tests: `SavesFreshExplicitBundle`, `RefusesHostAndUnverifiedSnapshot` | Real saved textures/Blueprint, job label and disk bundle. Tests use the private owned-fixture seam; accepted public child preflight/cook/Pak output remain separate gates. |
| `ConvaiAvatarPreparation.Copy` | Seven native strict guard/copy/save/unload/reload cases, including hard+soft and soft-only references | Strict shared API only; no default CPM facade, existing-destination V0 overwrite/move or engine-copy-policy regression proof. |

## Coordinator commands and prerequisites

Use the existing host `Saved/ConvaiAvatarStudio/Development/RunNativeTests.ps1`. It refuses to launch while any editor is open, checks a named successful build JSON, uses unique five-INI profiles, `-NoLiveCoding`, cook/MCP false for its owned editor, VibeUE preparation journal, `-TestExit=Automation Test Queue Empty`, unique report path, and exports actual results. Do not invoke `Automation RunTests ...;Quit`, which can exit before latent completion. Require actual current build/lastBuild success and matching source integration first; a successful stale build filename alone is insufficient.

Root-only commands, after replacing the build placeholder with its current successful build artifact and using a fresh report name:

```powershell
& './Saved/ConvaiAvatarStudio/Development/RunNativeTests.ps1' -Report 'S6V0FinalTests' -Build '<current-successful-build>.json' -Filter 'ConvaiPakManager.'
& './Saved/ConvaiAvatarStudio/Development/RunNativeTests.ps1' -Report 'S6PreparationFinalTests' -Build '<current-successful-build>.json' -Filter 'ConvaiAvatarPreparation.Copy'
```

The broad CPM suite already includes source policy, metadata slots, runner, both facade areas and local migration/delete-record behavior; do not repeat all narrow groups after a clean broad run without a new failure/change. Retain index.json, exact log, current build and workflow journal. Inspect every expected test path and warnings; never equate process exit zero or a nonempty report with complete coverage.

Existing available fixture prerequisites: real installed UE 5.8 editor/native AssetRegistry/AssetTools and project-built CPM/shared SDK; SDK mounted chatbot/MetaHuman animation assets used by the existing tests; current real owned actor/texture/package fixture patterns in preparation/selected-cook tests; root's fresh owned project and unique editor-profile setup in `owned_cook_gate.py` / `RunCookGate.ps1`. The five-case cook gate already checks native Entry cook and fail-closed host identity; it creates no A/B directory labels and does not inspect actual chunk Paks. Reuse its safe setup/journal patterns only after assigning ownership; rerunning it is not an all-label regression test.

## Missing native behavior to verify next

1. **Default CPM copy facade.** A small native owned-package case must call `FCPM_DependencyCopyAPI` with `bStrictValidation=false` and the legacy default hard+soft selection, save/unload/reload and check original/destination references plus returned item/remap/count fields. Existing strict fixture coverage bypasses this wrapper. Add separate meaningful native overwrite/move/engine policy cases only for preserved used V0 branches; do not count an options-equality test as behavior. No such test/filter exists in current CPM source.
2. **V0 fresh all-label packaging.** In a dedicated owned plain test project with no linked user/avatar/SDK content labels to rewrite, create A/B real assets and nonzero directory labels through native editor APIs. Save labels first, add/save one new asset under each, prove their persisted bundles are stale without resaving them, then invoke actual V0 `CPM_PackageProject` with a fresh output. Its unconditional pre-UAT save must update both persisted bundles. Require first non-iterative cook to produce both expected nonzero chunk Paks and verify entries using native UnrealPak `-List`; do not infer output from refreshed memory. This needs a buildable named native project target matching CPM's `-target=<project basename>` and UAT/Win64 packaging prerequisites. Existing `-run=Cook /Engine/Maps/Entry` fixtures alone do not prove full BuildCookRun target/stage/package feasibility. Run no Live Coding, no cloud jobs. Do not run all-label resave on the actual persistent linked uploader.
3. **V1 counterpart remains separate.** Run accepted `FCPM_AvatarCookInputs::Prepare` in the actual verified child, then a fresh A cook with B absent and linked original labels/content unchanged. The two selected-label native cases establish serialized explicit membership, not cooked isolation. Original V0 broad refresh must remain intact; removal is not the fix.
4. **Cook-only MCP.** For the real owned V0 packaging fixture, keep an owned editor's MCP listener active at a unique verified port and enable its local auto-start setting. Check actual UAT cooker arguments retain the exact false override, actual cook succeeds without a competing listener/error, original editor listener remains available and its config bytes remain unchanged. The current RunNativeTests/RunCookGate launch every owned editor with MCP false, so those runs cannot prove coexistence. No user listener/settings changes are required. No force-start CLI may be inherited by cook.
5. **Legacy cancel boundary and V1 containment.** In an owned native package fixture, distinguish the V0 CancelPublish terminal callback from actual UAT/root/descendant state; retain ownership until processes genuinely stop. Existing UAT notification cancellation can be observed independently, but is not a replacement assertion for CPM cancellation. The documented V0 lack of process cancellation must remain explicitly recorded. V1 must prove its own owned worker/UAT tree exits before lease release; currently WorkerProcess R3 runtime is on hold after a real grandchild-exit assertion failure. Do not launch a second job after the legacy logical Cancelled result to manufacture a concurrency pass.
6. **Cloud boundaries.** Backend create/publish/delete/same-ID update and source ownership/concurrency are not covered by these native/local tests. Run only previously authorized, contract-valid actual operations with fixed context through the eventual integration. Unsupported backend requirements remain S0 blockers; do not invent endpoints or use mocks as A11 cloud completion.

## Existing evidence inspected, not rerun

The actual reports at host `Saved/ConvaiAvatarStudio/Development` were read during this audit: `BaselineTests/index.json` has 72 clean + 2 warned successes, zero failed/notRun (74 total); `PreparationTests/index.json` has 78 clean + 2 warned successes, zero failed/notRun (80 total); `StudioLiveTests/index.json` has 18 clean successes, zero failed/notRun. These are earlier coordinator runs in this task, not fresh S6 final-source execution. The two baseline warned paths are `Avatar.AddsBothConvaiComponentsOnce` and `Avatar.WiresMetaHumanAnimBlueprints`; report entries show SDK Blueprint deprecations/component-template reconstruction. Preserve and compare actual warnings rather than masking the categories.

No native process, editor, test, build, backend request, source edit or Git mutation was performed by this auditor. S6/A11 remain partial until root executes the final native suite and the missing real packaging/facade checks, records their actual outcomes and keeps unresolved cloud/SDK/platform gates explicit.

## Bounded default-copy facade fixture — implementation, review item 1

Root subsequently assigned only new `Source/ConvaiPakManager/Private/Tests/CPM_DependencyCopyFacadeTest.cpp` (confirmed absent) and this ledger. This is a bounded test-only item with its own first independent review; it does not reopen completed source reviews or close broad S6. No production source, private SDK header, dependency/module, shared build or editor operation is changed.

Plan and base-derived expectations: call the public `FCPM_DependencyCopyAPI::CopyPackageWithDependencies` with an unmodified default `FCPM_DependencyCopyOptions`, explicitly verify strict remains false, then assert actual saved content/ref integrity and reflected report mappings. Read `staging@0218c2b` before selecting the existing-destination assertion: V0's game-package path filters out an existing destination even when `bOverwriteExisting=true`, marks it copied and subsequently fixes/saves destination references. It therefore preserves existing texture payload, not refreshed source payload. The fixture pins that existing behavior without claiming V1 incremental replacement. Already-under-destination inputs are separately skipped by the default `bCopyIfAlreadyInDestination=false`.

Implementation decisions:

- Reuse the existing SDK test-only native `ConvaiPreparationReferenceFixture` UClass through ordinary engine reflection, checking its hard-reference and FSoftObjectPath property types first. No private SDK header inclusion/export or second reflected test class is needed. This is real serialized UObject data; it is not a mocked copier. The fixture contains no calls to the strict/shared copy API.
- Reuse the existing preparation/selected-cook fixture pattern: unique GUID Saved directory and mounted package prefix, native SavePackage and synchronous registry scanning, strong object pins, exact owned-package cleanup, native UnloadPackages with `bResetTransBuffer=false`. Before reloading, require every owned package and previous nested weak UObject to be absent. No Undo reset or host setting/global rewrite is added.
- Fresh case uses distinct saved hard/soft target textures and a serialized root plus an unrelated sentinel. Require native registry edges before the operation; compare original/sentinel native file hashes afterward. Require all three legacy mount-preserving remaps, detailed item flags/counts and successful fixup. After actual unload, reload copied/original graphs and check hard identity, exact soft path/TryLoad and native texture source bytes.
- Existing case pre-saves destination textures with different pixels and a destination root still pointing at the originals. Default CPM copy must preserve those destination pixels while saving mapped references; originals/sentinel remain unchanged. It then freshly indexes the saved copies and calls the same CPM API with a root already inside the destination, checking three skips/no remap/no copied count and unchanged destination hashes. No destination whole-file hash is required across the fixup itself, since V0 intentionally resaves those packages.

Released native filters (not yet executed):

- `ConvaiPakManager.Preparation.DefaultFacade.SavesHardAndSoftReferences`
- `ConvaiPakManager.Preparation.DefaultFacade.PreservesExistingDestinationBehavior`

Root-only focused execution after an actual integrated successful build:

```powershell
& './Saved/ConvaiAvatarStudio/Development/RunNativeTests.ps1' -Report 'S6DefaultFacadeTests' -Build '<current-successful-build>.json' -Filter 'ConvaiPakManager.Preparation.DefaultFacade'
```

Both are also included in the existing broad `ConvaiPakManager.` filter. Source-only inspection performed; no build/test/editor/process or Git mutation run by implementer. Independent first review and real native results remain pending. Existing-destination preservation is legacy parity, not refresh correctness; move/engine-asset copy branches, full V0 UAT/label/MCP evidence and backend gates remain open.

### Fixture Round 2 response — actual compile failure

Coordinator's actual combined build failed at `CPM_DependencyCopyFacadeTest.cpp:302`: C2660, `CopyPackageWithDependencies` does not take two arguments. Re-read the public declaration at `CPM_DependencyCopyAPI.h:254-257`: the third options parameter has no default argument. Corrected only the already-under-destination call to pass the same existing const default-constructed `Options` used by the first call. No option values, assertions or production behavior changed. Root owns the fresh build and both native test runs; no execution or Git mutation by this response author. The failed build is not verification of either fixture.

## Default facade fixture - Round 1 independent review

Reviewer root, independent of pakmanager_s0. Source verdict approved for the two bounded native V0 facade regression cases; current build/runtime pending. Read the entire new fixture, CPM public defaults and wrapper, shared non-strict execution/fixup and prior staging behavior identified above. The fixture calls the real public CPM default API, preserves the legacy mount-based remap and existing-payload semantics, then verifies saved/reloaded hard/soft object identity and actual texture source pixels. Exact original and unrelated saved bytes are compared, and the already-inside destination call checks native skip/no-write behavior. It does not relabel strict/shared-only tests as V0 or treat skipped overwrite payloads as incremental preparation.

No must-fix source finding. Reusing the existing reflected preparation test class avoids another exported test type or module dependency. All created packages/mounts/files are scoped to a fresh GUID fixture; native unload explicitly retains global undo and checks every nested old UObject is absent before reload. Teardown marks only recorded mount-owned objects and limits deletion to its exact GUID directory. Actual runtime remains required, including failure-path diagnostics; these cases do not establish real MetaHuman/UAT packaging, all-label first cook, MCP coexistence or cloud contracts. Reviewed source SHA256356F90A9173B91ADC3048ADE469C03C1699A79016435C63465B20C217BCF1BDD.

## Default facade fixture - Round 2 independent review and native acceptance

Reviewer root. Verdict **approved**, two of three review rounds used. Read the corrected complete fixture and public declaration: the already-under-destination call now passes the same unmodified `Options`; no assertion or default behavior was weakened. Reviewed and executed source SHA256 `AE02B647102F847DBA51613905CA7E95F409253FAE5056603D9B293F4FDCFC5E`. Both native facade cases succeeded without warnings in `Saved/ConvaiAvatarStudio/Development/S6V0OriginalMonitorCookTests/index.json` after the real seven-action successful integrated build recorded in `cook-r2-original-monitor-v0-fixed-build.json` (2026-09-08T23:44:16Z). The preceding failed build remains recorded separately; it is not acceptance evidence.

The combined filter `ConvaiPakManager.+Convai.AvatarStudio.InputMonitor+Convai.AvatarStudio.CookRoles` discovered and completed 99 cases: 94 clean successes, five warned successes, zero failed/notRun. This comprises 86 CPM, seven input-monitor and six cook-role cases. The two new facade cases establish native saved/reloaded hard/soft references, actual texture payloads, exact original/sentinel preservation and the legacy destination no-op. The existing CPM avatar cases warned on existing SDK Blueprint deprecations. Three cook-role cases warned on deliberately unsupported ZenStore fixture settings and absent fixture cook-role state; these were inspected rather than suppressed. No error was reported.

`SelectedSource.ExportInteroperabilityFixture` explicitly skipped its opt-in producer export, so its successful no-op is **not** new archive interoperability evidence. Pure policy and fake-runner cases remain unit-level evidence, not backend/UAT integration. Original-only ancestor rename was prevented by Windows error 5, preserving native identity and bytes; that is prevention, not observed live replacement. Real all-label first UAT packaging, selected cooked Pak isolation, cook MCP coexistence, production preparation/lease integration and the real cloud create/update lifecycle remain open. S6 is partial.
