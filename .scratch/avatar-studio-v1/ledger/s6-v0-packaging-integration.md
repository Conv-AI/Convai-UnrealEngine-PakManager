# Avatar Studio V1 — real V0 packaging regression

## Item and constraints

- Item: `s6-v0-packaging-integration`, same Avatar Studio V1 task. Separate bounded fixture, default maximum three independent review rounds. Initial status: partial; no native execution yet.
- Authority: approved `Docs/Handoff/2026-09-08-avatar-studio-v1-design.md`, `Context/V1/Avatar-Studio-V1-Visual-Design.md`, `Context/V1/Avatar-Studio-V1-Implementation-Design.md` §§6.3/7/11–12 (A11, preserved all-label refresh and cook-only MCP override), and `Context/Claude-Code-PakManager-Context.md`. Meeting-selected child isolation remains separate G04/A07 work.
- Owner: PakManager implementer owns this ledger and one new `Source/ConvaiPakManager/Private/Tests/CPM_V0PackagingIntegrationTest.cpp`. Coordinator owns project setup, all builds/editor/UAT operations, independent review and commits. During the current canonical source freeze, the candidate is at `.scratch/avatar-studio-v1/candidates/CPM_V0PackagingIntegrationTest.cpp`.
- No production changes, fake jobs, server policy fetch, cloud mutation, source archive, Live Coding, selected-child fallback, global transaction reset, or test claim of UAT cancellation/drain.

## Round 1 - implementation

### Plan and decisions

1. Run only in a fresh, explicitly owned ordinary project: `<Host>/Saved/ConvaiAvatarStudio/Development/V0Packaging/<GUID digits>/AvatarStudioV0Packaging.uproject`. Require `-AvatarStudioV0Packaging`, exact absolute `-AvatarStudioV0PackagingHost=<Host>` and `-AvatarStudioV0PackagingProject=<Project>` plus an owned marker whose text is that exact canonical current project path. Reject commandlets, missing editor/Game target prerequisites, enabled Live Coding, linked project/plugin directories, previous cook/package/Game receipt and any pre-existing label before creating assets. Never run V0's broad label saver against the linked persistent uploader.
2. Use the native existing runner with exactly one real `UCPM_PackagePaksJob`: local explicit Windows/Development policy, Chunk 371, no existing-Pak reuse. The unchanged job reaches `CPM_PackageProject`, native all-label saving, and IUATHelper's real UAT invocation including `-build`. A new callback UObject or fake job is unnecessary. Native editor ticks drive completion.
3. Save A/B directory labels before adding fresh saved textures. Read serialized bundles using `IAssetRegistry::LoadPackageRegistryData` before and after the real job, without loading labels to infer persisted freshness. Require both A and B bundles refreshed even though the package job requests only A. Verify unchanged texture source files and valid native Game build receipt.
4. Open the two real cooked Pak indexes with `FPakFile`, without mounting them into the editor, and find exact A/B asset entries in their respective nonzero chunks with cross-avatar absence. Retain source assets, configuration and all cooked/build artifacts in the disposable project for independent inspection. No recursive cleanup is needed.
5. Capture only the real CPM packaging command log and assert its exact cook-only MCP argument, real build and Development target. Compare the owned editor's effective MCP values and INI snapshots. Coordinator separately verifies the real original-editor listener before/during/after packaging; source config alone is not listener evidence. No new MCP dependency.
6. A fixture deadline emits failure but does not claim to terminate/drain UAT or release fixture lifetime while the real job is outstanding. Coordinator's owned-process harness must supervise the real process tree; unchanged V0 cancellation cannot provide that proof.

### Required coordinator fixture (not yet executed)

- Create a new GUID directory at the exact path above. Physically copy the already verified local source plugin profile into this ordinary project (Convai, ConvaiHTTP, ConvaiPakManager and their current native prerequisites); do not copy `.writer.lock`, uploader receipts/journals, credentials, host config, links or host avatar content. This is a test project, not another production uploader/cache.
- Use a native minimal C++ runtime module and both `AvatarStudioV0PackagingTarget` (Game) and `AvatarStudioV0PackagingEditorTarget` (Editor), current UE 5.8 build settings. Build Editor through the coordinator's guarded real build. Leave the Game receipt absent: the real V0 UAT `-build` must produce the Game build. No `-skipbuild` or fake receipt.
- Enable the native ModelContextProtocol plugin in this owned fixture, with a coordinator-selected unused listener port and `bAutoStartServer=True` in the project's own EditorPerProjectUserSettings. Keep the real host listener untouched. All generated INI basenames used for this editor must resolve within the owned project. Use isolated editor layout/settings basenames as in the proven native fixture harness; do not suppress errors.
- `Config/DefaultGame.ini` must configure just `PrimaryAssetLabel` scanning `/Game/ConvaiV0Packaging` with native base `/Script/Engine.PrimaryAssetLabel`, `bHasBlueprintClasses=False`, `bIsEditorOnly=False`, `SpecificAssets=()`, default `CookRule=AlwaysCook`; no extra primary rules, scan exclusions, always-cook or never-cook directories. Set `bCookAll=False`, `bCookMapsOnly=False`, `bGenerateChunks=True`, `bGenerateNoChunks=False`, `UsePakFile=True`, `bUseIoStore=False`, `bUseZenStore=False`; cook only `/Engine/Maps/Entry`. Keep project startup/default maps at Entry and packaging free of additional maps. The candidate checks these prerequisites instead of rewriting configuration.
- Before launch, write UTF-8 marker `Saved/ConvaiAvatarStudio/Development/owned-v0-packaging-project.txt` containing the normalized absolute `.uproject` path. No existing `PackagedApp`, `Saved/Cooked`, `Saved/StagedBuilds`, fixture content, or Game receipt.
- Launch the ordinary owned editor (not `-run=...`) with `-NoLiveCoding -unattended -RenderOffscreen -nosound`, the three exact fixture arguments, `/Engine/Maps/Entry`, and `-ExecCmds="Automation RunTests ConvaiPakManager.Integration.V0.RealAllLabelPackaging" -TestExit="Automation Test Queue Empty" -ReportExportPath=<owned report>`. Do not force the parent editor MCP setting false; the real packaging code supplies the cook-only false override. Wrap unattended operations with the existing coordinator workflow journal.
- The test retains actual source and generated output; inspect both `PackagedApp/Windows/AvatarStudioV0Packaging/Content/Paks/pakchunk371-Windows.pak` and `pakchunk372-Windows.pak` with native `UnrealPak.exe <path> -List`, record UAT/Game build and child-cook logs, and probe the original fixture editor listener throughout. No backend/MetaHuman/selected-child/cancellation acceptance follows from this fixture.

### Verification

- Candidate implemented outside canonical Source during the coordinator's freeze. One native opt-in filter: `ConvaiPakManager.Integration.V0.RealAllLabelPackaging`; ordinary broad suites emit an explicit not-run warning unless the opt-in is present. The test retains its six fresh native UObjects and real source files through the package callback, then releases only its pins; it does not delete assets, mount Paks or touch transaction history.
- Static native-signature checks read UE 5.8 `PrimaryAssetLabel`, `AssetManagerTypes`, `IAssetRegistry`, `TargetReceipt`, `FPakFile`, `IPlatformFile`, `FOutputDeviceRedirector`, existing CPM package job/runner and the real UAT command builder. `FTargetReceipt` and `FPakFile` are existing CPM module dependencies. No added Build.cs dependency.
- Implementation/native build checks: not run by implementer, per serialized ownership. Coordinator must integrate the released candidate only after the Source freeze, build, and execute the real owned fixture. No current package, Game build, listener or Pak success claim.
- Independent review: pending Round 1.

## Round 1 - independent review

Reviewer root; actual candidate SHA256 `463EBFB0FCC34A26149A38ACC84E5ED708DB834061CFF13B87145019F085C95E`. Verdict **changes requested**, one of three rounds used.

Major: candidate unconditionally includes `Windows/WindowsHWrapper.h` and places Win32 HANDLE/DWORD calls under only WITH_AUTOMATION_TESTS. This new file would enter the existing cross-platform CPM editor module and break non-Windows compilation. Guard the Windows header with PLATFORM_WINDOWS and compile this Windows-native fixture body only when both WITH_AUTOMATION_TESTS and PLATFORM_WINDOWS hold, matching existing SDK native-fixture conventions. This does not broaden the V1 Windows profile; it preserves V0's existing platform build paths. No production/module dependency change is required.

Otherwise the bounded design is sound: exact physical GUID project/marker/target checks precede asset creation; no existing labels may enter the broad V0 resave; the real package job and native UAT callback remain in use. Saved registry bundles, Game receipt, Pak index membership, original texture bytes and exact cook argument are inspected. Actual parent MCP listener probing remains a root harness obligation, not an inferred pass from config. Native child execution, compilation and packaging are still unverified. Test deadline deliberately retains its owner while UAT runs; it must not be presented as cancellation or descendant-drain proof. No other must-fix source finding identified in this review.

## Round 2 - response

Guarded the Windows header with `PLATFORM_WINDOWS` and the entire native fixture body/registration with `WITH_AUTOMATION_TESTS && PLATFORM_WINDOWS`, following existing native SDK fixture conventions. No helper, assertion, UAT behavior, module dependency or production code changed. Current CPM descriptor also limits its editor module to Win64; the explicit guard makes this test's own native platform requirement independent of that descriptor and avoids imposing a future non-Windows compile failure.

Only the outside-Source candidate and this ledger changed during the canonical freeze. Actual compile/native execution remain not run by implementer and coordinator-owned. Round 2 source rereview is pending; this response does not extend or reset the three-round budget.

## Round 2 - independent review

Reviewer root. Source verdict **approved for the bounded Windows native fixture; real build/UAT/listener/Pak verification pending**. Two of three rounds used. Candidate SHA256 `3046289EB52AD3A61138AE7902A586186D4A970FFC6DB06053370F10F5990882` now guards both the Windows include and native implementation/registration. No assertion, ownership check or production behavior changed. The current module's Win64 descriptor restriction limits the immediate R1 impact, but explicit guards correctly preserve the fixture's native platform requirement without relying on that unrelated descriptor. No additional source finding. Runtime completion remains required before committing this test as verified work.

## Native coordinator harness plan — not executed

Coordinator assigned only two new host `Saved/ConvaiAvatarStudio/Development` scripts and this plan append. Their absence was checked before creation: `owned_v0_packaging.py` and `RunOwnedV0Packaging.ps1`. No production/test C++, build helper source, engine file or other ledger changed. Root owns script review and every execution. This is the same bounded fixture item; prior review history and the three-round limit are not reset.

The Python setup runs inside the original host editor and starts the real `WorkflowService` journal before copying. It requires an actual matching successful current child Editor build plus profile fingerprint artifact, a physical native uploader receipt, and the current V0 fixture source present in that child. It copies only receipt-listed source/content/resource/descriptor files for the three known plugins, including the already sanitized redirect default. It copies neither generated child settings, host settings/keys, links, receipts, journals nor caches. Installed TP_Blank Game/Editor/module text is renamed into the fresh ordinary project. It enables the installed native Python and MCP plugins for Editor targets and the native EnhancedInput dependency from TP_Blank. No VibeUE plugin is required in this fixture.

Copy verification streams the same source bytes to a new destination and checks SHA256, size, and source identity/mtime stability; the setup records these facts. It does not reimplement the receipt's BLAKE3 hash or pretend those per-file checks establish a concurrent global snapshot. Coordinator must first complete native source-profile verification and hold the source copy stable through setup. The current source child is still blocked by the real repair file-lock failure, so the new matching child build/profile prerequisites are unavailable. Old receipt/build artifacts must not be relabeled current or bypassed to run this fixture.

The generated project uses the exact native C++ fixture path/marker and packaging configuration above. The reviewed guarded build helper is physically copied to its own Saved directory as `GuardedBuildAndLaunchGame.ps1`; only `-Mode Development -NoLaunch` is exposed by the Build action. Its real no-editor-open check remains intact. Only Editor is built at setup: the Game receipt must remain absent until the existing V0 UAT job builds it.

The launch/evidence lifecycle has two exact nonce/PID/native-creation-FILETIME/project acknowledgements:

```mermaid
sequenceDiagram
  participant H as Host workflow / coordinator
  participant E as Owned plain editor
  H->>E: Launch, five unique INIs, NoLiveCoding
  E->>H: child-ready (real Slate ticks)
  H->>H: Native TCP listener belongs to exact editor PID
  H->>E: start-ack
  E->>E: Native real V0 automation/UAT
  H->>H: Sample listener and observed descendant identities
  E->>H: tests-complete + exact report SHA256
  H->>H: Probe listener AFTER package; native UnrealPak lists
  H->>E: Explicit exit-ack after inspecting evidence
  E->>E: DEFER QUIT_EDITOR
  H->>H: Observe actual editor exit; finish host workflow
```

Native API evidence was read before implementing the finalizer: `PySlate.cpp:123–155` registers/removes actual Slate post-tick callbacks; `KismetSystemLibrary.cpp:571–590` routes `execute_console_command` to `GEngine->Exec`; `UnrealEngine.cpp:5650–5652,6105–6109` defers commands; `EditorServer.cpp:5993–5996` handles QUIT_EDITOR. Native Python's own editor executor also defers QUIT_EDITOR at `EditorPythonExecuter.cpp:125`. The finalizer unregisters its callback and submits `DEFER QUIT_EDITOR` only after a matching final acknowledgement. No TestExit, Stop-Process, close-user-window, worker drain API, or timeout-driven closure is used. Failed startup/timeout remains inspectable and does not release or kill a process.

Coordinator commands, only after the current-child prerequisite is resolved (the example build/profile files must be the actual matching artifacts):

1. In the original host editor: `py E:/UEProjects/UE5.8/Dev_CPM_58/Saved/ConvaiAvatarStudio/Development/owned_v0_packaging.py setup child-mixed-delta-build.json child-mixed-delta-build-profile.json 18081`. Choose an unused listener port; the launcher checks the actual native Listen table before launching. Setup prints the fresh GUID and exact project. It does not launch or build.
2. After the host editor is closed through the coordinator's existing permission rules: `& ./Saved/ConvaiAvatarStudio/Development/RunOwnedV0Packaging.ps1 -Action Build -Id <fresh-guid>`.
3. `& ./Saved/ConvaiAvatarStudio/Development/RunOwnedV0Packaging.ps1 -Action Launch -Id <fresh-guid>`. It writes actual arguments and process identity, then returns with the owned editor waiting. Exactly five unique owned INI basenames are supplied; the parent's MCP setting is not forced false.
4. `& ./Saved/ConvaiAvatarStudio/Development/RunOwnedV0Packaging.ps1 -Action Observe -Id <fresh-guid> -TimeoutSeconds 1800`. It first verifies the real parent listener, acknowledges test start, samples it during work, verifies it after the actual report, and runs native UnrealPak `-List` for both retained chunks. It leaves the owned editor running. Listener evidence is native TCP socket ownership, not an invented protocol health check or descendant-drain proof.
5. Inspect the report, build, listener and Pak evidence, then explicitly run `& ./Saved/ConvaiAvatarStudio/Development/RunOwnedV0Packaging.ps1 -Action Acknowledge -Id <fresh-guid>`. It makes one more real listener probe, writes the exact exit acknowledgement and observes native editor exit for at most 60 seconds. Timeout never forces closure.
6. After reopening the original host through the coordinator's workflow: `py E:/UEProjects/UE5.8/Dev_CPM_58/Saved/ConvaiAvatarStudio/Development/owned_v0_packaging.py finish <fresh-guid>`. It attaches actual fixture/build/Game-receipt/report/listener/process/Pak artifacts and marks success only when the one exact native test, actual build, listener samples, native Pak listings and acknowledged editor exit succeed. Missing evidence remains failed.

Preparation status: scripts written and read for consistency only; no syntax checker, setup, build, launch, editor operation, native test, network probe or Git command executed by implementer. Independent script review and all runtime checks remain coordinator-owned. Current-child repair/build is an explicit blocker; V0 UAT, first Game build, MCP coexistence and actual chunk contents remain unverified.


## Round 3 - independent native harness review

Reviewer: sdk_s0, independent of the PakManager harness author. Source verdict: approved for the bounded coordinator-run V0 fixture harness; no must-fix finding. This is the third of three formal review passes for the existing item, including the prior C++ reviews. No review budget is reset. Actual setup, build, packaging, listener and Pak verification remain pending with the coordinator.

Independently verified both released source hashes: owned_v0_packaging.py C546DC259FC0B0F71313AF1593DC4B50EA64AF127D2EFF665576F9F0E20E5BCF and RunOwnedV0Packaging.ps1 46FD2A56449115E33D0DC262DBF2B9D4C2722D10C2C8FC881EC389B94F3E8B18. Read both complete scripts, the actual guarded BuildAndLaunchGame.ps1 copied by setup, installed TP_Blank project/targets/module, current plugin descriptors/provisioning source, native workflow/Python/automation/receipt APIs and the prior C++ ownership/configuration/real-job checks. Review performed read-only source inspection and this ledger append; no harness, syntax check, editor, build, native test, network/process probe, Git mutation or source edit was executed.

### Load-bearing checks

- Python setup derives the original host from unreal.Paths.project_dir and reads WorkflowService.get_environment. Actual VibeUEEnvironment.cpp:75-89 supplies engineRoot/engineAssociation; UWorkflowService_Journal.cpp:223-278 supplies success/runId. The source is the fixed physical uploader and inventory comes from its native receipt. Copy selection allows only the three reviewed plugin descriptors and Source/Content/Resources plus the sanitized ConvAI redirect default; host config, generated ConvaiEditorSettings.ini, uploader records, cache trees and links are excluded. Native user assets are physically copied as opaque source-profile bytes, never edited or manually interpreted.
- New GUID ownership, physical ancestor/file checks, exclusive new-file writes, byte-copy verification and retained incomplete fixture behavior are explicit. The project marker and setup bind the exact absolute project/host; the launcher rechecks these and the copied helper/Python/test hashes. The setup inventory records SHA256 and source size/identity/mtime checks. It does not validate the receipt's per-file BLAKE3 digests or establish an atomic whole-tree snapshot: the already documented coordinator prerequisite of native current-profile verification plus a frozen source through setup remains required. Old same-path build/profile artifacts cannot be relabeled current as a substitute.
- The installed TP_Blank Game/Editor targets use BuildSettingsVersion.V7 and Unreal5_8 include order; the module's EnhancedInput dependency is explicitly enabled. The actual copied build helper walks upward to the fresh fixture project, refuses any open editor and destructive clean flags, invokes real Build.bat for only the Editor target, and writes last-build.json. The wrapper exposes only -Mode Development -NoLaunch, then requires real matching build evidence. The Game receipt/cook/package directories must remain absent before launch; the existing native V0 test requires the real UAT -build and actual Game target receipt and Pak membership.
- The supporting-engine-plugin concern was checked against source rather than treated as a failure by analogy. Current provisioning itself emits only ConvAI/ConvaiPakManager/ConvaiHTTP in its C++ child project (AvatarStudioProvisioning.cpp:463-470); native descriptor/build resolution supplies its supporting closure. SDK ToolsetRegistry is optional, while the additionally enabled native ModelContextProtocol descriptor requires ToolsetRegistry non-optionally. PluginManager.cpp:2581-2593 explains optional filtering by the built receipt. This harness builds its own C++ Editor receipt; S0's earlier missing optional ToolsetRegistry case used a Blueprint-only engine receipt. No missing-dependency defect is established here. The actual fixture Editor build remains the required resolution evidence; no reduced-profile success is assumed.
- Launch uses one hidden owned editor, five unique fixture INI basenames, Entry, NoLiveCoding, an unused configured MCP port, and no TestExit or parent MCP-disable override. Native FTargetReceipt::LaunchesCurrentExecutable checks both Launch and LaunchCmd (TargetReceipt.cpp:239-250), consistent with the chosen UnrealEditor-Cmd executable.
- Ready/start/complete/exit signals bind nonce, exact project, PID and native creation FILETIME. Observe verifies the listener belongs to that process before acknowledging tests, samples while tests run, and probes again after the completed report; Acknowledge probes once more before its explicit quit acknowledgement. The Python callback unregisters itself and defers native QUIT_EDITOR only after matching acknowledgement. PySlate.cpp:123-155, UnrealEngine.cpp:6105-6108 and EditorServer.cpp:5993-5996 support these actual APIs. Timeouts retain the editor; there is no broad process kill, certificate change or download.
- Actual automation starts with HTML only, and normal JSON report export occurs on completion (AutomationControllerManager.cpp:390-394,1143-1155); the fresh report path plus inProcess/test checks do not pick up a prior run. Final success requires the exact one-test Success report and report hash, real build, native listener evidence, two successful native UnrealPak listings and acknowledged zero editor exit. The native C++ fixture supplies actual label freshness, Game-build and exact Pak entry assertions; listing exit codes alone are not substituted for those assertions.

### Acceptance still required

Execute only after the current source child is verified and frozen, then inspect the real Editor/Game build receipts, UAT and child-cook logs, saved-label assertions, exact Pak contents and before/during/after listener evidence. Listener samples prove observed native TCP ownership, not protocol health or uninterrupted availability between samples. Descendant enumeration is observational; it grants no termination authority and does not prove cancellation, complete process-tree accounting or drain. No V1 selected-child, source download, ownership or same-ID publication acceptance follows from this fixture.

Ledger released to the coordinator after this append. Further formal rework/review, if actual execution reveals a material issue, must follow the existing exhausted three-round workflow rule rather than silently opening another round.

### Coordinator actual execution and hold

The current physical source child was built successfully (child-mixed-delta-build.json, 310 actions), its mixed-mount native experiment passed, and PostLaunchSourceProfileTests verified reuse after startup. With source frozen, native setup created owned fixture ddb9a6b8280d455a98d9332393e3670e. Setup evidence is retained under its Saved/ConvaiAvatarStudio/Development directory; this is a real prepared fixture, not a packaging result.

The real fixture Editor build failed before compilation: UBT reported four action paths at 260-262 characters for ConvaiAvatarPreparationEditor dependency/object/DLL/library response outputs, then `Result: Failed (OtherCompilationError)` and exit6. Actual v0-packaging-editor-build.log and the fixture Saved/VibeUE/last-build.json remain failed. No Game build, UAT/cook, Pak listing or parent MCP listener verification ran. A shorter owned fixture path would address the observed build precondition without changing global long-path policy, deleting the retained fixture or moving the engine.

The three formal reviews are exhausted. Further path/harness rework is on hold pending the coordinator's consolidated user request for two additional rounds for this item, source-install verification and worker drain. Native owned_v0_packaging.py finish was executed in the original host on 2026-09-09: commandlet exit0, Workflow outcome failed, attaching the actual setup and failed build. The successful finalizer process is not a successful V0 fixture. Evidence: v0-failed-workflow-finish.log.
